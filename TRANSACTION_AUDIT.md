# 保存範囲の調査・改修 (Ver6.144 / 2026-09-08)

## 結果

`pos` / `pos-dev` 全体を読むトランザクションは全て撤去した。競合・権限拒否・旧ルール検出時に全体トランザクションへ戻る処理も撤去した。

`ref('/').update({ 'pos-dev/sessions/t1': value, ... })` は指定パスのみの一括更新であり、POS全体の読み書きではない。

## 変更した操作

| 操作 | 読み書きする範囲 |
| --- | --- |
| TC | 移動元と移動先の2卓、該当来店の付け回し、卓の追加・削除番号 |
| 場内指名の追加・明細削除 | 対象卓と関連付け回し |
| 会計確定 | 対象卓、同卓の有効な付け回し、関連キャストの出勤、会計履歴1件 |
| テーブル削除 | 対象卓、同卓の有効な付け回し、関連キャストの出勤 |
| 付け回し削除・時刻変更・終了取消 | 対象記録、必要な出勤・元の卓、同キャストの配席確認 |
| 出退勤・付け回しの通常保存 | 対象記録と同キャストの重複確認 |
| 会計履歴の支払変更・削除 | 会計IDで検索した履歴1件。旧配列形式にも対応 |
| 過去営業日の編集・削除 | 対象営業日とその一覧用サマリー |
| 過去営業日の日付変更 | 変更元・変更先の2日とサマリー |
| バックアップ復旧 | 選択された営業日・サマリー・必要な入退店履歴 |
| 営業開始・終了・再編集 | 対象営業日と当日の営業データ。終了時バックアップも一括保存 |
| 管理画面のリセット | 確認したセッション・付け回し・出勤。保存成功前に画面のデータを消さない |

## 競合対策

- `_scopedOperation` に対象記録のrevision・存在状態・来店識別情報を付け、更新・削除と同時にFirebaseルールで検証する。証跡は毎回置き換え、無制限に蓄積しない。
- 卓・キャスト別の追加削除番号を検索前に取得し、検索後に追加された付け回し・出勤も検出する。
- 営業履歴は削除後も営業日別の番号を残し、同日データの削除・再作成による古い操作の通過を防ぐ。サマリーも同じ番号で更新する。
- 会計・TCは未保存注文の完了後に最新セッションを複製する。失敗した注文が残る場合は処理しない。
- 会計IDは既存の表示・印刷に互換な安全整数を使用する。履歴の新規保存では既存キーがないことも検証する。
- 競合や権限エラーで無条件再送しない。成功表示は保存ACK後のみ。会計中の画面移動・モーダルを閉じる操作も禁止する。
- Firebaseが空配列・空オブジェクト・nullを省略する仕様を踏まえて営業日の内容を比較し、保存直後の偽の競合を防ぐ。

## 残すトランザクション

本体の `.transaction()` は2か所のみ。

1. `guardedSessionNodeTransaction`: 通常オーダーの卓1件。注文の競合検出に必要。
2. `saveGmsTargetCorrection`: 会計1件のGMS対象修正。revision照合に必要。

これらは全体トランザクションではなく、削除対象外。

## 検証

`node --test tests/*.test.js`。Firebaseルールも検証する場合はローカルDatabase Emulatorを127.0.0.1:9017で起動し、PowerShellで `$env:POS_RULES_EMULATOR='1'` を設定して実行する。テストは専用の `demo-` namespaceのみを使用し、実プロジェクトへ接続しない。

競合テストは会計・TC・付け回し追加・終了取消・出退勤削除・会計履歴編集・営業履歴移動/復旧・営業開始終了を対象とする。別卓の独立した保存が同時に成功することも検証する。

実行結果: エミュレーターを含む70テスト成功、全JavaScript構文確認成功。Playwrightで390/1024/1440pxのTC・会計モーダル、操作ロック、遅延表示、失敗からの再試行、完了後の遷移を確認した。

## 公開手順

1. 新ルールを適用する。devへの公開では取得した実ルールの `pos-dev` だけを置換し、`pos` / `access` / `backup` などは変更しない。
2. Ver6.144をdevへ公開する。
3. `pos-dev/_capabilities/scopedAtomicValidationVersion=614400` と `pos-dev/appVersion="6.144"` を設定する。既存のTC・営業日capabilityは維持する。
4. dev端末を再読み込みする。mainは別途明示的な公開指示があるまで変更しない。

capability未適用時は新方式の保存を停止する。旧方式へ戻さない。旧クライアント向けのルールは、mainと未更新環境への互換性のためcapability条件付きで残している。

実店舗の回線での完了時間は未計測。通信往復・未保存注文待ち・回線断の時間は残るため「即時完了」を保証するものではない。
